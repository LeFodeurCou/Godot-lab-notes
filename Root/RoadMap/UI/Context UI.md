#roadmap 

# TODO

- We can use flags in UIs, especially to force the player to make choises by example (like rewards, or a story non avoidable choice)
	- Can force the "escape" control to not close the UI until we make a choice
- Ensure the UI unicity to avoid opening them many times
	- Using a unique ID and a dictionary to store them
		- If the ID is already in the dictionary, we don't open the UI again