#roadmap 

# TODO

- Fix the wall / floor placement
	- Seen in debug camera, walls and floors are not perfectly aligned at the bottom
- Design a maze output
- Optimize 3D rendering
- See how to make a "land", like the ground of a zone (if no Minecraft style)
- Make a dual grid for better 3D placement
- Allow world build not only from floor and walls
- Use the seed handler to make some randomness by the TerrainRng object

# Next Step

- Define chunk sizes
- Define chunk number to load at a time
- Make the dual grid
	- The grid must extend until 
	- Make it visible in game with the debug mode
		- Can have many modes in debug
			- Display both grids
			- Display Cell Grid
			- Display Mesh Grid
			- Display Chunk grid (Cell grid but with only chunks visibles)