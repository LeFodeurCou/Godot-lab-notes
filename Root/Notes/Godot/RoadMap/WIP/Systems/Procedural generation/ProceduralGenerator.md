>A class that made a graph and a grid to organize data into a maze
>Using the Growing three algorithm

The graph is the list of [[Cells]] that are logically socketed with some other
The grid contains the very same [[Cells]] than the graph to ensure avoiding overlapping cells
Use a FrontierList to make the algorithm more performant
#proceduralGeneration 
