>A little class that contains the few things a Cell need to be use by the [[ProceduralGenerator]]

# Contains
- A relative position (X,Y)
- A content type that will be matched with the Godot nodes
- A socket list that contains what is needed to link cells / nodes between them
#proceduralGeneration 