Godot can handle:

```id="9fl8db"
1000–3000 nodes
```

without problems.

Performance problems usually appear above:

```id="ypl7yv"
10k nodes
```

Possible optimizations:

```id="rrqdt4"
world streaming
chunk loading
mesh merging
```

#node #optimization